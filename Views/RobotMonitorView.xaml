<UserControl x:Class="FactoryGuardian.Views.RobotMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!--헤더 -->
        <Grid Grid.Row="0" Margin="40,25,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="모니터링" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="26" FontWeight="SemiBold" VerticalAlignment="Center"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,40,0">
                <TextBlock Text="로봇 선택:" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox ItemsSource="{Binding Robots}" SelectedItem="{Binding SelectedRobot}" DisplayMemberPath="Name" 
                          Width="200" Height="34" FontSize="14" Background="{DynamicResource CardBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource BorderBrush}">
                    <ComboBox.Resources>
                        <Style TargetType="ComboBox">
                            <Setter Property="Padding" Value="10,5"/>
                        </Style>
                    </ComboBox.Resources>
                </ComboBox>
            </StackPanel>
        </Grid>    
    </Grid>

</UserControl>
